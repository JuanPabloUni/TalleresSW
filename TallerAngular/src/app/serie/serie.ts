export class Serie {
  constructor(public id: number, public nombre: string, public channel: string, public seasons: number, public descripcion: string, public webpage: string, public poster: string) {}
}
